---
date: 2026-04-04
day: Saturday
energy:
mood:
---

# Saturday, April 4, 2026

## Morning Check-In
> Start here first. Takes 3 minutes. Don't skip it.

**ONE Thing today:**

**Brain dump (what's on my mind right now):**
-

**Top 3 tasks (max 3 — pick carefully):**
- [ ]
- [ ]
- [ ]

---

## Log
> Drop everything here as the day happens. Notes, ideas, links, observations. No organization needed.

-

---

## Home / Family
> Anything the house or family needs today.

-

---

## Evening Close
> 5 minutes. Closes the day.

**Done today:**
-

**Unfinished (move to tomorrow or Inbox):**
-

**One thing I learned or noticed:**

**Tomorrow's ONE Thing:**

---

## Captured
> Raw capture bin — links, ideas, quotes to process later. Don't organize now.

---

## Vox Session Digest
> Written by Vox at session end. Do not edit manually.

### Context
Crash recovery Saturday. Computer crashed mid-session on 2026-04-03 while working on HookSnap Pro gating. Reconstructed full state from code files and .vox-working.md, then continued the build. Massive session: domain migration to hooksnap.app, Stripe Customer Portal for plan switching, endpoint naming (web + mobile), multiple bug fixes (wrong Vercel project, undefined slug in localStorage, name missing from API response, portal 500 from env var trailing newline, App Router cache staleness). All vault and memory files updated at session close.

### Decisions
- Dashboard feature (all-endpoints stats) will NOT be Pro-gated — available to all users
- Sideloading APK vs Play Store submission deferred — $25 Play Store fee is the gate, will decide when ready
- `BASE_URL` added as server-side Vercel env var (more reliable than `NEXT_PUBLIC_*` in API routes)
- Stripe billing portal proration enabled — plan switches charge annual minus credit for remaining monthly (no overlap)

### Facts Learned
- Vercel had two projects (`backend` + `hooksnap`) — was deploying to wrong one; fixed by relinking
- Next.js App Router caches pages client-side — `useEffect([], [])` doesn't re-run on back-navigation; fix with `visibilitychange` listener
- `NEXT_PUBLIC_BASE_URL` can carry trailing newline if set via `echo` pipe — causes "Not a valid URL" in Stripe portal
- Google Play Developer account is $25 one-time fee

### Related Projects
- HookSnap

### Keywords
#hooksnap #stripe #domain #mobile #vibe-coding #crash-recovery

---

## Vox Session Digest — Saturday Night

### Context
Late Saturday session (crossing into Apr 5). Finances updated from SoFi CSV drop, HookSnap DNS issue identified (www needs CNAME not A record), all uncommitted HookSnap changes pushed to git, admin stats endpoint built and deployed, HookSnap mode added to Vox Workspace.

### Decisions
- Workspace STORAGE_VERSION bumped to '7' for HookSnap mode addition
- Admin stats locked by `ADMIN_PRO_TOKEN` env var (token set in Vercel + hooksnap.env)
- HookSnap mode accent: emerald green (#4caf78) matching app icon
- Tab metadata fix committed separately — lesson learned: always commit all local changes before targeted push

### Facts Learned
- Upstash Redis `scan` returns cursor as `string`, not `number` — must cast with `Number(next)`
- All local HookSnap changes (Pro gating, above-fold, Settings, Privacy/Terms, PlanBadge) were uncommitted — pushing metadata-only commit triggered Vercel deploy from stale repo state
- www.hooksnap.app still has wrong DNS record type (A → 216.198.79.1); needs CNAME → e84f5298f13fb5bf.vercel-dns-017.com

### Related Projects
- HookSnap
- Vox Workspace
- Finances

### Keywords
#hooksnap #vox-workspace #finances #dns #admin

